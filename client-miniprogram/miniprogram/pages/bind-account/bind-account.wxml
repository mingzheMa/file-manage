<!-- pages/bind-account/bind-account.wxml -->
<van-field model:value="{{ mobale }}" label="手机号" placeholder="请输入手机号" />
<van-field model:value="{{ password }}" label="密码" type="password" placeholder="请输入密码" />
<van-button type="primary" block loading="{{commitLoading}}" bind:tap="onBindUser">提交</van-button>
<van-toast id="van-toast" />